# ── Backend ────────────────────────────────────────────────
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
GROQ_API_KEY=
GROQ_MODEL=openai/gpt-oss-120b
GEMINI_API_KEY=
SARVAM_API_KEY=

# ── Frontends ───────────────────────────────────────────────
# Doctor dashboard (server-side proxy target)
BACKEND_URL=http://127.0.0.1:8000
# Patient app (optional; defaults to the Expo dev machine's IP on port 8000)
EXPO_PUBLIC_API_URL=

# ── Backend CORS: comma-separated frontend origins ─────────
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:8081
