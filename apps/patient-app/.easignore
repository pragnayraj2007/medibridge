# What NOT to upload to the EAS build servers
node_modules/
.expo/
android/
ios/
web-build/
dist/
# Let EAS resolve dependencies itself (npm install, not npm ci)
package-lock.json
# Signing key stays on this PC; EAS receives it through the credentials channel
credentials.json
*.jks
*.keystore
*.bat
